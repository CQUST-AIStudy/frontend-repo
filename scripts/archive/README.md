# Archived Scripts

这里存放已执行过或仅用于历史诊断的一次性脚本。

- `repair-parse-damage.js`：历史编码/解析损坏修复脚本。
- `repair-utf8-missing-byte.js`：历史 UTF-8 缺失字节修复脚本。
- `inspect-parse-context.js`：历史解析上下文诊断脚本。

当前仍在使用的脚本保留在 `scripts/` 根目录，例如 `switch-api.js`。
