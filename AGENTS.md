# AGENTS.md

## 语言

- 所有说明、分析、计划、总结、测试结果统一使用中文。
- 默认优先中文提问、中文回复。
- 代码标识符、库名、接口字段名、命令参数可保留英文。

## 项目范围

- 本文件适用于 `frontend-repo` 前端项目。
- 默认只修改前端代码与前端配置。
- 若任务涉及后端接口、数据库、Docker 或部署配置，必须先说明边界；没有明确要求时，不修改后端仓库。
- 修改前先阅读相关页面、组件、路由、状态管理、请求封装与配置文件，避免基于猜测直接改代码。

## 工作原则

- 优先最小必要改动。
- 优先复用现有实现、现有组件、现有工具函数和项目约定。
- 不修改与当前任务无关的文件。
- 不进行无必要重构，不为“顺手优化”扩大改动范围。
- 遇到不确定信息，先核对项目现状，再继续。
- 不硬编码敏感信息，例如 API Key、Token、数据库凭据、私有服务地址。

## 终端与环境

- 默认使用 `pwsh` 执行 PowerShell 命令。
- 默认使用 UTF-8 编码。
- 注意中文文件名、中文路径、中文输出的兼容性。
- 在 Windows 环境下避免依赖 bash 特有行为，除非项目明确支持。

## 依赖管理

- 严格使用 `npm`，以 `package-lock.json` 作为唯一锁文件。
- 禁止使用 `pnpm` 或 `yarn`，不得新增 `pnpm-lock.yaml` 或 `yarn.lock`。
- 非必要不新增依赖；能用现有依赖实现的，优先不引入新包。
- 新增依赖前先评估是否存在项目内替代方案。
- 安装依赖统一使用：

```bash
npm install
```

- 如确需新增依赖，使用：

```bash
npm install xxx
npm install xxx --save-dev
```

- 前端脚本统一使用 `npm run <script>`，例如 `npm run serve`、`npm run lint`、`npm run build`。

## Vue 3 约定

- 当前项目以 Vue 3 + JavaScript SFC 为主，优先保持现有 JS 风格；除非项目明确迁移 TypeScript，不强行改成 TypeScript。
- 优先使用 Composition API。
- 优先使用 `<script setup>`。
- 优先复用现有组件、composables、stores、工具函数和样式体系。
- 页面必须考虑以下状态：
  - Loading
  - Error
  - Empty State
- 表单、列表、弹窗、分页等常见交互优先遵循项目现有模式。
- 避免在组件中堆积过多业务逻辑，能拆则拆。
- 页面组件优先负责组合数据、状态和布局；复杂逻辑应放入普通 JS 工具模块、composable 或子组件。

## 路由与页面

- 新增页面时同步检查：
  - `src/router/index.js`
  - 对应角色的 Layout 菜单
  - 面包屑文案
  - 权限或路由守卫
- 修改已有入口文案时，应确认桌面端菜单、移动端菜单和面包屑是否一致。
- 不删除旧入口或旧页面，除非用户明确要求。

## API 联动

- 接口变更必须同步检查：
  - 前端调用
  - 请求封装
  - 页面展示
  - 错误处理
  - mock、示例数据或文档
- 不允许留下前后端不一致的状态。
- 若后端接口不存在，前端只能生成、校验、预览请求 payload，不应声称已经真实写库或真实提交成功。

## AI / LLM

- Prompt、配置、解析逻辑分离。
- 不静默修改模型输出格式。
- 不硬编码 API Key、Token、模型路径或私有配置。
- 涉及模型输出解析时，优先增加显式校验与失败兜底。

## 验证

完成修改后，尽可能执行与当前改动最相关的验证。

```bash
npm run lint
npm run build
```

- 若项目新增 `type-check` 脚本，也应执行：

```bash
npm run type-check
```

- 未验证不得声称已修复。
- 无法验证时必须说明原因。
- 若验证失败，先定位失败原因，再决定是否继续修复。

## Ignore 约束

- 每次结束任务前，必须检查前端项目根目录下是否存在 `.gitignore` 和 `.dockerignore`。
- 若 `.gitignore` 或 `.dockerignore` 不存在，则创建对应文件。
- 若已存在，则根据本次任务更新内容。
- 必须将本次任务产生的、无需纳入 Git 版本管理或 Docker 构建上下文的文件、目录、缓存、日志、环境变量文件等写入对应 ignore 文件。
- 若本次任务没有新增需要忽略的内容，也要确认现有 ignore 已覆盖项目常见临时产物，避免遗漏敏感配置、依赖目录或构建产物。

## 禁止事项

- 不伪造测试结果。
- 不硬编码敏感信息。
- 不新增占位实现来冒充完成。
- 不引入无必要的破坏性改动。
- 不随意升级无关代码。
- 不为完成任务而绕过已有规范。
- 不在缺乏依据时推断项目约定。
- 不使用 `pnpm` 或 `yarn` 管理依赖。

## 回复格式

每次回复尽量使用以下结构：

### 改动概述

说明本次做了什么、为什么这么改。

### 修改文件

列出实际改动的文件。

### 关键实现

说明关键逻辑、设计取舍或联动影响。

### 验证结果

说明执行了哪些验证、结果如何。

### 风险说明

说明仍可能存在的风险、限制或待确认项。
